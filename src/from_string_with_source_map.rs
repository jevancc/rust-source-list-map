use source_node::SourceNode;
use code_node::CodeNode;
use source_list_map::SourceListMap;
use mappings_context::MappingsContext;

pub fn from_string_with_source_map(code: &str, map: MappingsContext) -> SourceListMap {
}

fn add_code(generated_code: String) -> &str {
}
